sudo: false

language: 
  - python

python:
    - "3.3"
    - "3.4"
    - "3.5"
    - "3.6"

before_install:
    - "pip install coveralls"
    - "pip install --no-cache-dir -r requirements.txt"

install:
    - "python setup.py install"

after_success:
    - coveralls